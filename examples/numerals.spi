
Load nat.

numeral : Type₁.
numeral =  (A : Type) -> (A -> A) -> A -> A.

zero : numeral.
zero = \A f x => x.

one : numeral.
one = \A f x => f x.

two : numeral.
two = \A f x => f (f x).

three : numeral.
three = \A f x => f (f (f x)).

plus : numeral -> numeral -> numeral.
plus =
  \m n =>
    \A f x =>
      m A f (n A f x).

Check plus.

Eval plus three two Nat Succ Zero.
